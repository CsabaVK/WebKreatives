node_modules/
nul
.env.leads
*.env

# Internal handover / onboarding docs — never publish to the public repo
HANDOVER.md
START-HERE.md


# Internal tooling, notes and lead data — never publish to the public repo
tools/
leads/
memory/

# These were committed before the rules above existed, so the rules never
# took effect and the files stayed tracked in a public repo. Untracked now;
# they remain on disk. Note the history still holds them.
CLAUDE_MEMORY.md
tmp-inspect-*.cjs

# Rendered social images, regenerated from _design/ on demand
_design/out/

# Client print renders, regenerated from templates/print/specs on demand
templates/print/out/
__pycache__/
_design/horizonx/
